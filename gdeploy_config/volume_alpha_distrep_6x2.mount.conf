# USM QE alpha configuration for GlusterFS
#
# This gdeploy config file assumes that we have:
# * 1 client machine (GlusterFS client)

[hosts]


[clients]
volname=volume_alpha_distrep_6x2
action=mount
fstype=glusterfs
client_mount_points=/mnt/volume_alpha_distrep_6x2
hosts=
